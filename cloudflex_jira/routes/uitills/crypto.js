const crypto = require('crypto');export default function emailCrypto(ty,pw,key) {    if(ty ==='enc'){        const encrypt = crypto.createCipher('des', key)        const encryptResult = encrypt.update(pw, 'utf8', 'base64')            + encrypt.final('base64')        return encryptResult    }else{        const decode = crypto.createDecipher('des', key)        const decodeResult = decode.update(pw, 'base64', 'utf8')            + decode.final('utf8')        return decodeResult;    }}